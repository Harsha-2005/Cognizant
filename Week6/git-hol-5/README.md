# Git HOL 5 — Cleanup & Push

Follow `5. Git-HOL.docx` to:

- Ensure working tree is clean, pull latest from remote, and push any pending changes.
- Verify remote reflects the pushed commits.

Quick commands:
```
git status
git pull origin master
git push origin master
```
